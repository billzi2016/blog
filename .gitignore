# 意图：忽略本地系统文件、编辑器状态和 Hugo 构建产物，保持仓库只记录源码与内容。

# Hugo 构建输出，可由 `hugo` 重新生成，不应提交。
/public/
/resources/_gen/
/.hugo_build.lock

# 本机环境文件可能包含隐私或密钥，必须排除。
.DS_Store
.env
.env.*

# 编辑器状态与个人偏好无关项目行为，不纳入版本控制。
.idea/
.vscode/
*.swp
*.swo

# 运行日志属于临时产物，不作为源码提交。
*.log
