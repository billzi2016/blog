# Agent 工作规则

## 项目纪律

- 本项目必须优先遵守 `specs/` 下的规格文档；规格文档是架构、范围和实现取舍的依据。
- 不允许为了快速交付绕过规格、跳过实现、制造 mock、伪造验证结果或留下不清楚的临时逻辑。
- 如发现规格和实现冲突，必须先说明冲突，再按规格修正实现；不能自行篡改规格来迁就已有代码。
- 项目必须持续遵循 SOLID 和 DRY 原则，避免重复逻辑、隐式耦合和难以维护的临时分支。

## 路径与隐私

- 项目文件、文档、提交信息和说明中禁止写入本机绝对路径。
- 所有路径都应使用相对路径，避免泄露电脑用户名、目录结构或本机环境信息。
- 如必须描述位置，只能使用仓库内相对路径，例如 `content/posts/`、`specs/tasks.md`。

## 代码与注释

- 每个代码或配置文件开头必须有中文意图注释，说明该文件负责什么。
- Hugo 模板、样式和工作流中的关键逻辑必须有中文注释，说明维护时需要注意的约束。
- 对长难句、关键条件、容易误改的配置项必须补充清晰中文注释。
- 注释要服务维护，不写空话，不写“显而易见”的机械解释。

## 修改方式

- 修改或新增文件必须使用 patch 模式。
- 不要用整体重写代替局部修改，除非文件超过 90% 都需要变化。
- 开工前必须说明准备生成、修改或删除哪些文件，以及为什么要动它们。
- 不得删除或移动文件，除非用户明确要求并确认影响范围。

## Git 提交

- commit 信息必须使用中文。
- commit 信息必须清晰、具体、可审查，描述本次变更的真实范围。
- commit 正文不超过 10 行。
- 不允许把多个无关模块塞进同一个提交。
- 不允许主动 push，除非用户明确要求。

## 质量底线

- 不允许 mock 数据冒充真实数据。
- 不允许跳过实现、跳过验证或伪造测试结果。
- 不允许为了速度制造多套逻辑，导致后续无法判断哪套才是正确实现。
- 如必须留下后续任务，必须写入 `specs/tasks.md`，并说明原因与边界。
